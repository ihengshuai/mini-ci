{
  "$schema": "https://raw.githubusercontent.com/iamyoki/better-scripts/main/lib/schema.json",
  "dev": {
    "alias": "Dev",
    "desc": "正在启动本地开发服务",
    "command": "turbo run dev --filter=!examples --filter=!docs"
  },
  "build": {
    "alias": "Build",
    "desc": "正在打包...",
    "command": "turbo run build --filter=!examples --filter=!docs"
  },
  "docs:dev": {
    "alias": "Docs:dev",
    "desc": "正在启动本地文档",
    "command": "pnpm --filter docs dev"
  },
  "docs:build": {
    "alias": "Docs:build",
    "desc": "正在打包文档",
    "command": "pnpm --filter docs build"
  },
  "test:unit": {
    "alias": "Unit Test",
    "desc": "单元测试",
    "command": "jest --config jest.config.ts"
  }
}
