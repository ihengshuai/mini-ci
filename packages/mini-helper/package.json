{
  "name": "@hengshuai/mini-helper",
  "version": "0.0.1",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "build": "esbuild --bundle ./src/index.ts --format=cjs --outfile=./dist/index.js --platform=node --minify  && tsc -p ./tsconfig.build.json && yalc publish",
    "test:unit": "jest --config jest.config.ts"
  },
  "dependencies": {
    "@hengshuai/mini-type": "workspace:^0.0.1",
    "chalk": "^5.3.0",
    "esbuild": "^0.20.1",
    "open": "8.4.2"
  }
}
